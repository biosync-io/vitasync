---
title: Progressive Web App (PWA)
description: Install, offline support, push notifications, background sync, and PWA configuration for the VitaSync dashboard.
---

import { Aside, Steps, Tabs, TabItem } from '@astrojs/starlight/components';

The VitaSync web dashboard is a full-featured **Progressive Web App** — installable on desktop and mobile, offline-capable with intelligent caching, and push-notification enabled.

## Features

| Feature | Technology | Browser Support |
|---------|-----------|----------------|
| **Installable** | Web App Manifest | Chrome, Edge, Safari 17+, Firefox (Android) |
| **Offline caching** | Workbox (service worker) | All modern browsers |
| **Push notifications** | VAPID / Web Push | Chrome, Edge, Firefox, Safari 16.4+ |
| **Background sync** | Background Sync API | Chrome, Edge |
| **Periodic sync** | Periodic Background Sync | Chrome, Edge |
| **App badge** | Badging API | Chrome, Edge, Safari (macOS) |

## Setup

The PWA works out of the box with no extra configuration needed. The service worker is automatically generated during `next build` by [`@ducanh2912/next-pwa`](https://github.com/AnaArsonist/next-pwa).

### Push Notifications (Optional)

To enable push notifications, generate VAPID keys and add them to your environment:

<Steps>

1. **Generate VAPID keys**

   ```bash
   pnpm --filter @biosync-io/web generate-vapid-keys
   ```

   This outputs a public/private key pair.

2. **Add to environment**

   ```bash
   # .env
   NEXT_PUBLIC_VAPID_PUBLIC_KEY=BEl62iU...
   VAPID_PRIVATE_KEY=UUxI4o8...
   VAPID_SUBJECT=mailto:admin@vitasync.io
   ```

3. **Restart the web service**

   ```bash
   docker compose up -d web
   ```

   Users will be prompted to allow notifications when they visit the dashboard.

</Steps>

<Aside type="tip">
  VAPID keys only need to be generated once. The same key pair should be used across all environments and deployments. Changing keys will invalidate all existing push subscriptions.
</Aside>

## Architecture

```
Browser
  ├── Web App Manifest (/manifest.json)
  │     └── Defines app name, icons, start URL, display mode
  ├── Service Worker (/sw.js) — generated by next-pwa + Workbox
  │     ├── Precache: Next.js build output (pages, JS, CSS)
  │     ├── Runtime cache: API responses, fonts, images
  │     ├── Push handler: shows notifications, handles clicks
  │     ├── Background sync: replays offline mutations
  │     └── Periodic sync: refreshes health data every 12h
  └── Client App
        ├── useInstallPrompt — captures beforeinstallprompt event
        ├── useOnlineStatus — tracks online/offline state
        ├── usePushSubscription — manages push subscription lifecycle
        ├── useBadge — updates app icon badge count
        └── usePeriodicSync — registers periodic background sync
```

## Caching Strategy

The service worker uses different strategies depending on the resource type:

| Resource | Strategy | Cache Name | TTL |
|----------|----------|------------|-----|
| Next.js build output | **Precache** | `workbox-precache` | Until next build |
| Icons (`/icons/*.png`) | **CacheFirst** | `icon-cache` | 30 days |
| Google Fonts CSS | **StaleWhileRevalidate** | `google-fonts-stylesheets` | — |
| Google Fonts files | **CacheFirst** | `google-fonts-webfonts` | 1 year |
| API data (`/api/v1/*`) | **NetworkFirst** | `api-cache` | 5 min, 10s timeout |
| Next.js static (`/_next/static/*`) | **CacheFirst** | `next-static` | 1 year |
| Next.js data (`/_next/data/*`) | **StaleWhileRevalidate** | `next-data` | 1 day |
| Images (jpg, png, webp, etc.) | **CacheFirst** | `image-cache` | 30 days |

<Aside>
  API data uses **NetworkFirst** with a 10-second timeout. If the network is slow or unavailable, the cached response is returned immediately. This means dashboard charts and metrics can display stale data while offline — better than showing nothing.
</Aside>

## Offline Mutations

When the device is offline, write operations (POST/PUT/DELETE to `/api/v1/*`) are queued in **IndexedDB** under the `vitasync-offline` database. When connectivity returns, the service worker replays them in order via the Background Sync API.

Supported offline actions include:
- Logging water intake
- Recording mood entries
- Writing journal entries
- Updating habit check-ins

<Aside type="caution">
  Background Sync is only supported in Chromium browsers (Chrome, Edge). On Safari and Firefox, offline mutations are stored but won't auto-replay — the user needs to retry manually when back online.
</Aside>

## Custom Service Worker

The custom service worker code lives in `apps/web/service-worker/index.ts`. It is merged with the Workbox-generated service worker by `next-pwa` at build time. It handles:

- **Push events** — parses notification payload and shows a system notification
- **Notification clicks** — opens or focuses the relevant dashboard page
- **Background sync** — replays queued offline mutations from IndexedDB
- **Periodic sync** — refreshes health scores, readiness, and insights

## Icons

PWA icons are generated from `apps/web/src/app/icon.svg` using a sharp-based script:

```bash
pnpm --filter @biosync-io/web generate-icons
```

Generated icons in `apps/web/public/icons/`:

| File | Size | Purpose |
|------|------|---------|
| `vitasync-72.png` | 72×72 | Notification badge |
| `vitasync-180.png` | 180×180 | Apple touch icon |
| `vitasync-192.png` | 192×192 | PWA icon |
| `vitasync-512.png` | 512×512 | PWA splash / install |
| `vitasync-maskable-192.png` | 192×192 | Maskable (safe zone) |
| `vitasync-maskable-512.png` | 512×512 | Maskable (safe zone) |

## Hooks Reference

| Hook | File | Purpose |
|------|------|---------|
| `useOnlineStatus` | `lib/hooks/useOnlineStatus.ts` | Returns `boolean` — reactive online/offline tracking via `useSyncExternalStore` |
| `useInstallPrompt` | `lib/hooks/useInstallPrompt.ts` | Returns `{ canInstall, isInstalled, promptInstall, dismiss }` — captures `beforeinstallprompt` |
| `usePushSubscription` | `lib/hooks/usePushSubscription.ts` | Returns `{ isSubscribed, isSupported, subscribe, unsubscribe }` — manages VAPID push |
| `useBadge` | `lib/hooks/useBadge.ts` | Updates app icon badge with unread count via Badging API |
| `usePeriodicSync` | `lib/hooks/usePeriodicSync.ts` | Registers periodic background sync for health data refresh |

## Components

| Component | File | Description |
|-----------|------|-------------|
| `OfflineBanner` | `lib/components/OfflineBanner.tsx` | Floating pill at bottom-center — amber when offline, green "Back online" toast on reconnect |
| `InstallPrompt` | `lib/components/InstallPrompt.tsx` | Card in bottom-right with Install/Not now actions; remembers dismissal in localStorage |

## Environment Variables

| Variable | Required | Description |
|----------|----------|-------------|
| `NEXT_PUBLIC_VAPID_PUBLIC_KEY` | For push | Public VAPID key (shared with browser) |
| `VAPID_PRIVATE_KEY` | For push | Private VAPID key (server-side only) |
| `VAPID_SUBJECT` | For push | Contact URI (`mailto:` or `https:`) |

## Troubleshooting

### Service worker not registering
- Service workers only work over **HTTPS** or on **localhost**
- In development, the service worker is **disabled by default** (set `disable: false` in `next.config.mjs` to test)
- Check DevTools → Application → Service Workers for registration status

### Push notifications not working
- Verify VAPID keys are set in environment variables
- Check that the user granted notification permission (DevTools → Application → Notifications)
- On iOS, push notifications require **Safari 16.4+** and the app must be **added to Home Screen**

### Offline page showing unexpectedly
- Clear the service worker cache: DevTools → Application → Storage → Clear site data
- Rebuild the app to regenerate the service worker: `pnpm --filter @biosync-io/web build`

### Icons not displaying
- Regenerate icons: `pnpm --filter @biosync-io/web generate-icons`
- Verify files exist in `apps/web/public/icons/`
